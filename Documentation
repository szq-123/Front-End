Common Browsers: Firefox, Chrome, Safari, Opera, Edge.

Kernel:
1. read webpage's content, run scripts and display the page. like language runtime.
2. instances: Trident, Blink, Webkit.

Web Standard:
1. W3C.
2. Make different browsers generate the same style of webpage when parsing same source code.
3. Structure: HTML. Presentation: CSS. Behavior: JS.

element: an item with paired tags. <p>content</p>
attribute: usually described in opening tag. specifies the properties of the element. <a href=''>

block-level element: usually starts with a new line. always takes up the full width available. <div>, <p>, <h1>,

inline element: not start with a new line. takes up as much width as necessary. <span>, <td>