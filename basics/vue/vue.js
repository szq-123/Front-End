// JS framework. simplify DOM operations.
// see more at https://vuejs.org/

// vite: a build tool.
// compared to `webpack`, the build tool when using `vue create demo` in vue-cli.
// vite is more efficient (display code changes faster).
// because vite only rebuild modified pages.